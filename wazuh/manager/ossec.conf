<ossec_config>

  <!-- ── CONFIGURACIÓN GLOBAL ── -->
  <global>
    <jsonout_output>yes</jsonout_output>
    <alerts_log>yes</alerts_log>
    <logall>no</logall>
    <logall_json>no</logall_json>
    <email_notification>no</email_notification>
    <smtp_server>smtp.example.wazuh.com</smtp_server>
    <email_from>wazuh@example.wazuh.com</email_from>
    <email_to>recipient@example.wazuh.com</email_to>
    <email_maxperhour>12</email_maxperhour>
    <email_log_source>alerts.log</email_log_source>
    <agents_disconnection_time>10m</agents_disconnection_time>
    <agents_disconnection_alert_time>0</agents_disconnection_alert_time>
  </global>

  <!-- ── ALERTAS ── -->
  <alerts>
    <!-- Nivel mínimo para guardar en el log -->
    <log_alert_level>3</log_alert_level>
    <!-- Nivel mínimo para enviar al Indexer (OpenSearch) -->
    <email_alert_level>12</email_alert_level>
  </alerts>

  <!-- ── INTEGRACIÓN CON EL BACKEND SOC ── -->
  <!-- Cuando una alerta supere nivel 7, la envía al backend -->
  <integration>
    <name>custom-soc-backend</name>
    <hook_url>http://host.docker.internal:8080/api/v1/alerts/ingest</hook_url>
    <level>7</level>
    <alert_format>json</alert_format>
  </integration>

  <!-- ── CONEXIÓN CON WAZUH INDEXER ── -->
  <indexer>
    <enabled>yes</enabled>
    <hosts>
      <host>https://wazuh.indexer:9200</host>
    </hosts>
    <ssl>
      <certificate_authorities>
        <ca>/etc/ssl/root-ca.pem</ca>
      </certificate_authorities>
      <certificate>/etc/ssl/filebeat.pem</certificate>
      <key>/etc/ssl/filebeat-key.pem</key>
    </ssl>
  </indexer>

  <!-- ── MONITOREO DE LOGS DEL SISTEMA ── -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/ossec/logs/active-responses.log</location>
  </localfile>

  <!-- ── REGLAS ── -->
  <ruleset>
    <decoder_dir>ruleset/decoders</decoder_dir>
    <rule_dir>ruleset/rules</rule_dir>
    <rule_exclude>0215-policy_rules.xml</rule_exclude>
    <list>etc/lists/audit-keys</list>
    <list>etc/lists/amazon/aws-eventnames</list>
    <list>etc/lists/security-eventchannel</list>
  </ruleset>

  <!-- ── API DEL MANAGER ── -->
  <api>
    <https>yes</https>
  </api>

  <!-- ── DETECCIÓN DE VULNERABILIDADES ── -->
  <vulnerability-detection>
    <enabled>yes</enabled>
    <index-status>yes</index-status>
    <feed-update-interval>60m</feed-update-interval>
  </vulnerability-detection>

</ossec_config>